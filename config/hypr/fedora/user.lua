-- Your preferences. Installer reruns preserve this file.
require("fedora.input")
require("fedora.appearance")
require("fedora.animations")
require("fedora.keybinds")
-- Optional clamshell handling; enable after checking normal suspend/lid behaviour.
-- require("fedora.clamshell")
-- Override examples (replace existing binds first):
-- hl.unbind("SUPER + Return")
-- hl.bind("SUPER + Return", hl.dsp.exec_cmd("konsole"), { description = "Terminal" })
